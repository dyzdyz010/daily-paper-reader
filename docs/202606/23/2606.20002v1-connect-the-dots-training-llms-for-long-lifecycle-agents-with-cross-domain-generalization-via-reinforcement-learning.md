---
title: "Connect the Dots: Training LLMs for Long-Lifecycle Agents with Cross-Domain Generalization Via Reinforcement Learning"
title_zh: 连点成线：通过强化学习训练大型语言模型实现长生命周期智能体的跨域泛化
authors: "Yanxi Chen, Weijie Shi, Yuexiang Xie, Boyi Hu, Yaliang Li, Bolin Ding, Jingren Zhou"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20002v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 通过强化学习训练LLM智能体在长任务序列中持续更新上下文（长期记忆），实现逐步改进。
tldr: 针对长生命周期LLM代理需持续学习环境的问题，提出Connect the Dots (CoD)框架，通过端到端强化学习训练LLM交替解决任务与更新环境上下文。使用GRPO式RL及细粒度信用分配，实验验证该训练方式能有效激发元能力，并显示出在训练域内、跨域乃至Ralph-loop设置下的分布外泛化潜力。这一框架为构建自适应AI代理提供了新方向，并已开源。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20002v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1658, \"height\": 833, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1608, \"height\": 492, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1523, \"height\": 1466, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1527, \"height\": 1463, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.20002v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1360, \"height\": 1272, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.20002v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1561, \"height\": 1453, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 815, \"height\": 814, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.20002v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 545, \"label\": \"Table\"}]"
motivation: 现有LLM代理缺乏长生命周期下的持续学习与自适应元能力，难以在动态环境中渐进提升。
method: 提出CoD框架，设计端到端RL训练，交替执行任务求解与上下文更新，并采用GRPO式算法与细粒度信用分配。
result: 实验表明，CoD训练能激发LLM的元能力，并在域内、跨域及Ralph-loop设置上实现分布外泛化。
conclusion: CoD框架为长生命周期代理训练提供新范式，有望推动LLM代理的通用化与自适应发展，已开源实现。
---

## 摘要
本研究提出了一个通用框架，用于训练大型语言模型（LLMs）具备“连点成线”（Connect the Dots, CoD）能力——这是长生命周期智能体所需的一项元能力：当一个基于LLM的AI智能体部署到环境中时，它会解决一长串任务，同时持续探索环境、从自身经验中学习，并迭代地自我更新其关于环境的上下文，从而在更新后的上下文条件下，在未来任务中逐步取得更好的表现。CoD框架的主要组成部分包括：（1）面向端到端强化学习（RL）的算法设计与基础设施，其中长序列展开由交替出现的任务解决片段和上下文更新片段组成；（2）用于在训练过程中激励和引出LLM目标元能力的任务和环境，以及用于在评估时忠实度量进展的任务和环境。我们给出了CoD框架的概念验证实现，包括一种具有细粒度信用分配的GRPO风格RL算法，以及为引出目标元能力（而非领域特异的LLM能力或标准的逐任务RL）而定制的任务和环境。实证结果验证了在CoD设定下端到端RL训练的有效性，并展示了所引出的元能力在分布外泛化上的潜力——包括训练域内、跨不同域、以及从CoD到Ralph-loop设定。我们对CoD的探索连接了多条先前工作脉络，并为推进LLM和AI智能体的发展开辟了新机遇。为了促进进一步的研究和应用，我们在https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod 发布了实现。

## Abstract
This work presents a general framework for training large language models (LLMs) to "Connect the Dots" (CoD), a meta-capability required by long-lifecycle agents: as an LLM-based AI agent gets deployed in an environment, it solves a long sequence of tasks while continuously exploring the environment, learning from its own experiences, and iteratively self-updating its context about the environment, thereby achieving progressively better performance on future tasks conditioned on the updated context. Major components of the CoD framework include: (1) algorithm design and infrastructure for end-to-end reinforcement learning (RL) with long rollout sequences interleaving solve-task and update-context episodes; (2) tasks and environments for incentivizing and eliciting the targeted meta-capability in LLMs during training, as well as for faithfully measuring progress during evaluation. We present proof-of-concept implementations of the CoD framework, including a GRPO-style RL algorithm with fine-grained credit assignment, as well as tasks and environments tailored to the targeted meta-capability (rather than domain-specific LLM capabilities or standard task-by-task RL). Empirical results validate the efficacy of end-to-end RL training in the CoD setting, and demonstrate the potential for out-of-distribution generalization -- within the training domains, across different domains, and from CoD to Ralph-loop settings -- of the elicited meta-capability. Our investigation of CoD connects several lines of prior works, and opens up new opportunities for advancing LLMs and AI agents. To facilitate further research and applications, we release our implementations at \url{https://github.com/agentscope-ai/Trinity-RFT/tree/research/cod/examples/research_cod}.

---

## 论文详细总结（自动生成）

好的，根据您提供的论文内容，以下是对该论文的结构化、深入、客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

*   **核心问题**: 当前大型语言模型在作为AI智能体进行长生命周期部署时，缺乏一种关键能力：在与环境持续交互解决一系列不同但相关的任务时，能够**主动、有意识地探索环境、从自身经验中学习，并迭代更新其关于环境的上下文（即“记忆”或“经验”），从而在未来任务中表现得越来越好**。论文将这种元能力定义为 **“连点成线” (Connect the Dots, CoD)**。
*   **研究背景与动机**:
    *   尽管LLM已展现出强大的推理和工具使用能力，但在信息不完备、需要持续探索和适应的动态环境中，它们仍显不足。它们容易迷失或依赖预先设计好的复杂智能体脚手架。
    *   标准的**逐任务强化学习 (Task-by-task RL)** 仅训练模型从零开始独立解决每个任务，与长生命周期部署所需的自适应元能力不匹配。
    *   论文受到“终身智能体”、元强化学习等领域的启发，认为需要通过一种**专门设计的端到端强化学习范式**，直接训练LLM掌握这种CoD元能力，从而从根本上提升智能体的自适应和长期性能。

### 2. 论文提出的方法论：核心思想、关键技术细节

论文提出了一个名为 **CoD** 的通用框架，包含部署（CoD-Deploy）和训练（CoD-Train）两个核心组成部分。

*   **核心思想**: **将LLM的RL训练提升至更高的层级**。传统RL是单任务内的“回合”，而CoD-RL的展开轨迹（Rollout）是一个由**任务解决片段**（Solve-task episodes）和**上下文更新片段**（Update-context episodes）交替组成的**长任务序列**。模型被训练去最大化当前任务和未来任务的累积奖励。

*   **关键技术细节**:
    *   **RL算法：GRPO风格的细粒度信用分配**:
        *   在CoD-Train设定的长轨迹中，每个任务解决片段会获得一个结果奖励，而上下文更新片段仅因没有直接奖励而获得一个微小的格式奖励。
        *   遵循动态规划原则，为每个片段定义**回报 (Return)**：即该片段及之后所有未来任务解决片段的平均奖励。
        *   采用**多点信用分配**：将不同展开轨迹中处在**相同位置（位置索引）** 的片段归为一组，计算组内平均回报作为基线，来估算每个片段的优势函数 (Advantage)。
        *   这种方法使得上下文更新片段也能因促进未来任务的成功而获得正向优势。
    *   **梯度计算的稳定性增强**:
        *   基于`REC-OneSide-NoIS`变体（舍弃可能不稳定的重要性采样权重），并引入了一种**自适应重加权启发式方法**。当批次的平均优势为负时，通过自适应调整温度参数`T`对优势值进行指数级重加权，以平衡正负梯度更新的比例，抑制训练不稳定问题。公式被修改为：
            `g := 1/N * Σ_i ∇_θ log π_θ(i) · A_i · exp(A_i / T) · M_i`
    *   **上下文机制**: 当前实现采用一种极简的“**线索 (Hint)**”机制，即一段由LLM自己总结的文本，会附加到后续任务解决片段的系统提示中。这是对更复杂的持久记忆库等形式的概念验证。

### 3. 实验设计：使用数据集/场景、基准、对比方法

*   **训练与评估环境**: 论文设计了三个专门的环境，旨在激励CoD元能力，而非测试特定领域知识。
    *   **`FrozenLake-Obscure`**: 经典“冰湖”游戏的变体。动作对应的实际方向（上/下/左/右）在每次新环境中是未知且随机排列的。智能体必须通过试错探索出正确的映射关系，这构成了**信息论极限**，迫使模型必须依赖上下文。
    *   **`Alchemy-Random`**: 炼金术合成游戏的变体。每次新环境的元素组合配方是随机的。智能体需要探索并记录有效的合成路径。
    *   **`TerminalSimulator`**: 一个模拟终端操作的场景，用于跨域OOD评估。任务涉及文件操作等。

*   **训练设置**:
    *   **模型基座**: `Qwen3-8B-Instruct`（关闭思维链功能）。
    *   **设置A**: 仅在`FrozenLake-Obscure`域上进行CoD-Train训练，任务序列长度为4。
    *   **设置B**: 在`FrozenLake-Obscure`和`Alchemy-Random`的混合域上进行CoD-Train训练，任务序列长度也为4。

*   **评估设置（OOD泛化）**:
    *   **域内泛化**: 在与训练相同的环境下，但使用**更困难的实例**（如更大的地图、更多元素）和**更长的任务序列**（长度为8）进行评估。
    *   **跨域泛化**: 在`设置A`训练的模型，除了在`Alchemy-Random`上评估，还在`TerminalSimulator`上进行评估。
    *   **范式泛化 (至Ralph-loop)**: 在`TerminalSimulator`上，不仅测试在CoD-Deploy（序列不同任务）下的表现，还测试在**Ralph-loop**（对同一任务反复尝试）下的表现。

*   **对比方法（消融实验）**: 在附录A中，对比了三种用于梯度计算的RL算法变体：
    *   标准GRPO方法（式1）。
    *   `REC-OneSide-NoIS`方法（式2，舍弃IS权重的变体）。
    *   本文最终采用的算法（式3，在`REC-OneSide-NoIS`基础上增加自适应重加权）。

### 4. 资源与算力

*   **说明**: 论文全文**未明确提供**进行实验所使用的GPU型号、数量、训练时长或总计算量等具体的算力资源信息。仅提供了一个指向开源代码的Github链接。

### 5. 实验数量与充分性

*   **主要实验组数**: 论文设计了两个主要的端到端RL训练实验（设置A和设置B），并分别在这两个设置下进行了详细的评估。
*   **评估维度**:
    *   对每个训练设置，都提供了**训练奖励曲线**（展示不同任务位置的奖励变化）。
    *   对每个训练设置，都进行了**多维度的OOD评估**，并绘制了对应的评估奖励曲线，包括：域内（困难环境）评估、跨域评估、以及范式泛化（Ralph-loop）评估。
*   **消融实验**: 在附录A中提供了一个关于**RL算法稳定性**的对比实验（图5），对比了三种梯度计算方法在`Alchemy-Random`环境下的训练奖励曲线和平均优势值变化。
*   **充分性与客观性分析**:
    *   **充分性**: 实验设计具有逻辑性，从单域训练到混合域训练，并从多维度验证了元能力的涌现和泛化。但总共仅有两个主设置和三种环境，实验数量相对有限，特别是在混合域训练（设置B）上观察到了训练不稳定的迹象，表明该方法的可扩展性仍需更大规模的验证。
    *   **客观性**: 论文选择的基线是未经CoD训练的同一模型(`Qwen3-8B-Instruct`)本身，这是一个公平的起点。其主要目标是概念验证，旨在展示通过精心设计的RL范式可以引出CoD元能力，这一点通过“训练后显著优于初始模型”的结果得到了证明。但它没有与其他可能提升长周期代理性能的基线方法（如仅优化提示、仅增加上下文长度等）进行对比。

### 6. 论文的主要结论与发现

*   **CoD元能力可被端到端RL激发**: 通过CoD-Train，LLM能够学会在任务序列中有效地进行上下文维护和利用，使得在**任务序列的后半段性能，远超从零开始解决任务的性能**。例如，在`FrozenLake-Obscure`实验中，序列中第4个任务的成功率从28%大幅提升至76%，而初始任务成功率仅从18%提升到45%（受信息论极限制约）。
*   **所激发的元能力具有显著的泛化潜力**: 即使仅在`FrozenLake-Obscure`上训练，模型也展现出在三个层面上的OOD泛化能力：
    *   **域内泛化**: 在相同领域但更困难的环境和更长任务序列上，性能同样能持续提升。
    *   **跨域泛化**: 在未见过的 `Alchemy-Random` 和 `TerminalSimulator` 环境上，模型性能也得到了改善。
    *   **范式泛化**: 在 `TerminalSimulator` 的Ralph-loop（反复尝试同一任务）设定下，模型同样能通过迭代自我改进获得更高奖励。
*   **专门的任务和环境设计至关重要**: 标准RL环境无法有效训练出这种元能力。必须设计具有“信息隐晦性”的环境，迫使智能体为了获得高奖励而必须进行探索、学习和上下文转移。

### 7. 优点：方法或实验设计上的亮点

*   **新颖的问题定义与范式**: 明确提出了CoD这一元能力，并设计了与之精确匹配的、层级更高的RL训练范式（CoD-Train），这是对传统任务级别RL的重要扩展。
*   **精巧的RL算法适配**: 将GRPO算法适配到长序列多回合场景，并提出了**基于位置索引的分组信用分配**方法，有效地解决了延迟奖励和上下文更新片段的激励问题，是一个关键技术贡献。
*   **面向元能力的环境设计**: 设计的`FrozenLake-Obscure`和`Alchemy-Random`环境，巧妙地剥离了领域特定知识，专注于测试和训练纯粹的探索-利用-学习循环，是优秀的元学习测试床。
*   **对训练稳定性的改进**: 提出的**自适应热平衡重加权机制 (Adaptive RED-Weight)** 为解决大规模RL训练中因正负样本不平衡导致的训练不稳定问题，提供了一个简单而有效的启发式方案。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

*   **概念验证阶段，规模有限**: 实验仅在`Qwen3-8B`这一种规模的模型上进行，且仅使用了两个核心训练域。该方法在更大规模模型、更广泛和复杂的真实场景下的有效性有待考证。
*   **简化的上下文管理机制**: 当前实现中的“Hint”仅是一段简短的文本摘要，对于更复杂的现实任务（如长期代码库开发、个性化助理）可能远远不够，未能探索持久化记忆、结构化知识库等更强大的机制。
*   **缺少与相关方法的直接对比**: 论文没有与那些同样旨在提升LLM在长期、探索性任务中表现的方法进行对比，例如更复杂的推理时扩展（Inference Scaling）策略、或使用反思机制的RL方法。
*   **训练稳定性问题未彻底解决**: 在混合域训练（设置B）中，`Alchemy-Random`的奖励曲线出现了波动和性能回退，表明当前RL算法在处理多领域混合训练时可能面临挑战，距离稳定、大规模的训练尚有距离。
*   **关键算力信息缺失**: 全文未提及任何算力开销，使得其他研究者难以复现或评估其资源需求。
*   **泛化根源有待深究**: 对于在`FrozenLake-Obscure`上训练获得的元能力为何能泛化到结构迥异的`TerminalSimulator`场景，论文承认“需要进一步调查以确定根本原因”，这仍然是一个开放问题。

（完）
