---
title: Self-Compacting Language Model Agents
title_zh: 自压缩语言模型智能体
authors: "Tianjian Li, Jingyu Zhang, William Jurayj, Xi Wang, Chuanyang Jin, Mehrdad Farajtabar, Eric Nalisnick, Daniel Khashabi"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23525v1"
tags: ["query:agent-memory"]
score: 8.0
evidence: 通过模型驱动的压缩决策管理LLM智能体上下文窗口的自压缩框架
tldr: "大型语言模型智能体在长程推理中，因思维链与工具调用轨迹累积陈旧信息，易超出上下文窗口。现有固定间隔压缩无视任务结构，常打断推导或丢弃部分结果。本文提出SelfCompact，通过让模型使用压缩工具并结合轻量触发规则实现自适应压缩，无需额外训练。在六个基准测试中，以30-70%更低成本超越固定间隔压缩与无压缩基线，数学任务最高提升18.1分，智能体搜索任务提升5-9分，表明规则能弥补模型自行判断上下文时效的元认知不足。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23525v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 855, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23525v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1426, \"height\": 717, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.23525v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1446, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.23525v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 467, \"height\": 186, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1405, \"height\": 820, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 692, \"height\": 244, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1470, \"height\": 897, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1473, \"height\": 285, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 622, \"height\": 282, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1446, \"height\": 211, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 707, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1082, \"height\": 762, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.23525v1/table-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1346, \"height\": 1213, \"label\": \"Table\"}]"
motivation: 长程智能体轨迹因陈旧信息累积而性能下降，固定间隔压缩无视任务结构，易丢失关键推导。
method: 提出SelfCompact，模型结合压缩工具与轻量触发规则，在推理中自适应压缩上下文，无需训练。
result: "在六个基准上，SelfCompact匹配或超越固定压缩，数学最高+18.1分，搜索+5-9分，成本降30-70%。"
conclusion: 本研究揭示模型无法自主感知上下文腐烂的元认知鸿沟，通过轻量规则驱动压缩，无需训练即可大幅提升长程推理性能。
---

## 摘要
由思维链和工具调用构成的冗长智能体轨迹会积累过时内容，锚定后续生成，并最终超出上下文窗口。现有框架通过固定间隔压缩在令牌阈值触发来缓解此问题，但这种触发方式无视轨迹结构，有可能在推导或搜索过程中丢弃部分结果。我们提出 SelfCompact，这是一种让模型自行决定何时以及如何压缩的框架。具体而言，它结合了两个推理时元素：(i) 一个压缩工具，模型可调用该工具来总结累积的上下文；(ii) 一套轻量级规则，指明何时触发（子任务已解决或轨迹收敛）及何时抑制（推导中途或陷入困境时）。两者缺一不可。单独的工具在不同开源模型中使用不均，常在无用时刻被调用或根本未被调用；而单独的规则无法执行动作。两者结合，无需任何微调或外部监督，即可激发有效的自适应压缩。我们在六个基准（竞争性数学与智能体搜索）和七个模型上给出了实证结果。结果表明，SelfCompact 在远低于固定间隔总结的令牌成本下，性能与之相当或更优：在数学任务上比无总结基线最高提升 18.1 分，在智能体搜索任务上提升 5-9 分，且每问题成本降低 30-70%。我们的结果揭示了一个元认知差距：尽管未经提示的模型无法可靠判断其自身上下文何时变质，但一套轻量级规则弥补了这一差距，将何时压缩重新理解为一种无需训练即可由框架提供的能力。

## Abstract
Long agent traces composed of chains of thought and tool calls accumulate stale content that anchor subsequent generations, and eventually outgrow the context window. Existing scaffolds mitigate it with fixed-interval compaction triggered at a token threshold. Such triggers pay no heed to trajectory structure, risking discard of partial results mid-derivation or mid-search. We propose SelfCompact, a scaffold that allows the model itself to decide when and how to compact. Specifically, it pairs two inference-time elements: (i) a compaction tool the model invokes to summarize the accumulated context, and (ii) a lightweight rubric specifying when to fire (a sub-task has resolved, or the trajectory is converging) and when to suppress (mid-derivation, or when stuck). Both are needed. The tool alone is unevenly used across open-weight models, often invoked at unhelpful moments or not at all; the rubric alone cannot act. Together, they elicit effective adaptive compaction without any fine-tuning or external supervision. We present empirical results on six benchmarks (competitive math and agentic search) and seven models. Our results show that SelfCompact matches or exceeds fixed-interval summarization at a fraction of the token cost, improving over a no-summarization baseline by up to 18.1 points on math and 5-9 points on agentic search at 30-70% lower per-question cost. Our results expose a meta-cognitive gap: although unprompted models cannot reliably tell when their own context is rotting, a lightweight rubric closes this gap, reframing when to compact as a capability that scaffolds can supply without training.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

- **研究背景**：随着大语言模型（LM）被用于解决更长、更复杂的任务，其生成的推理轨迹（包含思维链和工具调用）不断增长，可达数万至数百万 Token。
- **核心问题**：漫长的轨迹会积累大量“陈旧信息”（如早期的错误分支、无效搜索结果等），这些内容不仅占据上下文窗口，还会像锚一样拖累后续生成，导致模型性能严重下降，即“上下文腐烂”现象。
- **现有方法局限**：当前工业界和学术界的主流做法是**固定间隔压缩**（如每达到一定Token阈值就触发一次总结）。这种方法“盲目”地执行压缩，完全无视轨迹的推理结构，可能在模型推导中间或搜索过程中丢失关键部分结果，造成不可逆的损害。
- **研究动机**：探究大型语言模型智能体能否在无需训练的情况下，自行识别其自身的上下文腐烂，并在恰当的时机进行压缩。

### 2. 论文提出的方法论

论文提出了一种名为 **SelfCompact** 的训练无关框架，其核心思想是将“何时压缩”的决策权交还给语言模型自身，但通过一个轻量级规则来引导其决策。该框架结合了两个关键元素：

- **压缩工具**：提供一个内联的`<summarize>`工具，模型可以在生成过程中主动调用它来总结当前的上下文轨迹。该总结器与被评测的模型是同一个，无需外部验证器。
- **轻量级触发规则 (Rubric)**：这是一套详细的、针对具体任务的自然语言指令。它在固定的探测间隔（例如，每生成 N 个 Token 或每进行 M 次工具调用）被追加到上下文末尾，要求模型根据轨迹的当前状态（如“子任务是否结束”、“是否陷入僵局”、“是否正在推导中”）给出二值判断：`COMPRESS`（压缩）或 `CONTINUE`（继续）。

- **算法流程**：
  1.  模型在上下文中进行正常的推理和工具调用。
  2.  当达到预设的探测间隔 `N` 时，框架暂停主任务生成，并追加一个包含规则的“探针”提示。
  3.  模型读取当前完整轨迹，并依据规则提示，输出 `COMPRESS` 或 `CONTINUE` 的裁决。
  4.  **若裁决为 `COMPRESS`**：追加一个总结提示，让模型对被暂停的轨迹进行总结，生成紧凑的摘要。之后，用该摘要替换原有的完整轨迹，模型从“原始问题 + 摘要”的新上下文继续生成。
  5.  **若裁决为 `CONTINUE`**：丢弃探针提示和裁决，模型从原上下文继续生成，仿佛无事发生。
  6.  **效率设计**：通过复用KV缓存，规则探测几乎零成本，而总结调用也只产生一次性的开销，后续所有生成都基于大幅缩短的摘要进行，从而显著降低了总成本。

### 3. 实验设计

实验覆盖了**竞争性数学**和**智能体搜索**两大类任务，共6个基准，7个模型。

- **任务场景与基准**：
  - **数学推理**：
    - 数据集：IMO-Answerbench, HMMT Nov 2025, HMMT Feb 2026。
    - 模型：Qwen3-4B-Instruct, Qwen3-30B-A3B-Instruct, Qwen3.5-4B, Qwen3.5-9B（禁用思维模式）。
  - **智能体搜索**：
    - 数据集：BrowseComp, BrowseComp-Plus, DeepSearch QA。
    - 模型：GLM-4.7-Flash, MiniMax-M2.5, MiMo-V2-Flash。

- **对比方法**：
  - **无压缩 (No Compaction)**：不进行任何上下文管理，直至超出窗口或达到最大步数。
  - **固定间隔总结 (Fixed-interval summary)**：当上下文达到最大窗口的30%时触发压缩。
  - **全部删除 (Delete-all)** / **保留最后N轮 (Keep-last-N)**：作为辅助对比的简单基线。
  - **消融实验 (Ablation)**：将 SelfCompact 中移除规则，仅保留压缩工具，让模型自行决定何时压缩。

### 4. 资源与算力

- **模型推理**：
  - 对于数学推理任务，模型使用 vLLM 框架部署在单张 H200 GPU 上进行推理（30B-A3B 模型使用 2 路张量并行）。
  - 对于智能体搜索任务，通过 OpenRouter API 调用托管模型，未提及特定的本地算力部署。

- **训练情况**：本文方法完全**无需微调**，没有消耗任何训练算力，所有操作均在推理时完成。摘要中也明确指出了这一局限，即未探索通过强化学习将规则内化到模型权重中的可能性。

### 5. 实验数量与充分性

- **实验数量**：
  1.  **主实验**：在6个基准、7个模型上与多种基线方法进行了充分对比，共计数十组（模型×基准）实验。
  2.  **消融实验**：对规则的有效性进行了消融研究，对比了有/无规则下的性能。
  3.  **深入分析**：包括（a）**预言机分析**，揭示了固定间隔压缩仍有巨大改进空间；（b）**难度分层分析**，证明 SelfCompact 在更难的问题上增益更大；（c）**触发时机分析**，量化了 SelfCompact 与固定间隔的触发时间点差异；（d）**定性案例分析**，通过具体轨迹展示了不同压缩策略的决策差异。

- **评估充分性**：实验设计全面、对比公平（尤其在数学实验中严格控制了Token预算）。多维度（性能、成本、错误模式、效率）的评估体系充分验证了方法的有效性。

### 6. 论文的主要结论与发现

- **性能与效率兼得**：SelfCompact 在数学和智能体搜索任务上，以**30-70% 更低的每问题Token成本**，实现了与固定间隔总结相当或更优的性能。
  - 数学：比无压缩基线最高提升 **18.1 分**。
  - 搜索：比无压缩基线提升 **5-9 分**。
- **元认知鸿沟与弥补**：模型自身无法可靠地判断何时应该压缩，但通过一个精心设计的、轻量级的规则提示，可以在不进行任何训练的情况下，成功引导模型做出恰当的压缩决策，从而弥补这一差距。
- **规则的必要性**：消融实验表明，仅提供压缩工具而不提供规则，模型的使用行为极不稳定，性能甚至会降到与固定间隔相当或更差，证明了规则指导是成功的关键。

### 7. 优点

- **创新性强**：首次提出并验证了在训练无关的设置下，基于规则的、自适应的压缩时机决策，重新定义了“何时压缩”这一元认知能力。
- **实用价值高**：方法轻量、无需训练，可与任何模型即插即用，且在不牺牲甚至提升性能的同时大幅降低了推理成本，为长程智能体任务提供了高效的解决方案。
- **分析深刻**：通过预言机分析、难度分层分析等手段，不仅展示了“效果好”，更深入探究了“为什么好”，指明了固定间隔压缩失败的具体模式和改进空间。
- **兼容性好**：可与现有KV缓存技术良好结合，通过复用缓存，使额外的规则探测成本几乎可忽略不计。

### 8. 不足与局限

- **模型覆盖范围**：仅在开源模型上进行了评估，未能验证该方法在面对前沿闭源模型（如GPT-5.5, Claude Opus）时，其带来的增益是否会缩小（因为这些模型可能已具备更强的元认知能力）。
- **规则通用性**：当前的触发规则是为特定任务（数学、搜索）手工设计的，其设计细节和普适性未经验证，应用到全新领域时可能需要重新设计。
- **未探索学习范式**：该方法固守在推理时，未探索通过强化学习（RL）等后训练方式，将这种由规则指导的外部能力内化为模型自身的策略，这可能进一步降低成本并提高鲁棒性。
- **局限性分析**：论文明确指出其范围仅限于训练无关的干预，以隔离规则的独立贡献，未来将RL视为自然延伸。

（完）
