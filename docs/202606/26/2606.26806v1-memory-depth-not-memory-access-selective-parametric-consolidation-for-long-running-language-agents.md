---
title: "Memory Depth, Not Memory Access: Selective Parametric Consolidation for Long-Running Language Agents"
title_zh: 记忆深度而非记忆访问：面向长期运行语言智能体的选择性参数巩固
authors: Haoliang Han
date: 2026-06-25
pdf: "https://arxiv.org/pdf/2606.26806v1"
tags: ["query:agent-memory"]
score: 10.0
evidence: 提出EVAF，一种选择性参数巩固机制，为长运行智能体提供持久记忆深度
tldr: 长期语言代理需持久目标倾向，而非仅事实检索。现有检索无法决定卸载后哪些经验持续影响行为。本研究定义记忆深度问题，提出EVAF机制（惊讶-价态门控LoRA巩固）。在loop-drift测试中，EVAF以每200事件2-3次参数写入，实现0.812-0.904目标持久性，分解为选择与驱动两可控维度。记忆深度与检索互补，为长期代理提供重要方向。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26806v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1587, \"height\": 663, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.26806v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 482, \"height\": 243, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26806v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1437, \"height\": 407, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26806v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 848, \"height\": 171, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.26806v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1017, \"height\": 476, \"label\": \"Table\"}]"
motivation: 现有检索无法决定卸载后哪些经验应持续影响行为，需记忆深度。
method: 提出惊讶-价态门控LoRA（EVAF）巩固机制，在loop-drift协议下评估。
result: EVAF仅2-3次参数写入即获0.812-0.904目标持久性，分解为选择与驱动双维度，揭示模型依赖写入强度。
conclusion: 记忆深度与检索互补，EVAF证实了选择性参数巩固对长期代理的必要性。
---

## 摘要
长期运行的语言智能体需要的不仅仅是记忆访问。检索系统可以在查询时获取过去的事实，但它们并不决定在卸载工作上下文后，哪些经验应继续塑造行为。我们将这个独立的问题视为记忆深度：写入小型参数存储中的持久目标条件化倾向。我们引入了循环漂移协议，一种受控的压力测试，其中检索索引保持完整，而工作上下文被卸载，目标条件化行为必须在长循环干扰下持续。我们评估了 EVAF，一种基于惊讶和情感效价门控的 LoRA 巩固机制。在 GPT-2 和 TinyLlama 上，检索在浅层事实回忆方面表现最强（短事实准确率 0.956--0.973），而 EVAF 在目标持久性和卸载后恢复方面表现最强（0.812--0.904），每 200 个事件仅需 2--3 次参数写入。机制控制表明，选择性巩固可分解为两个可控维度：选择和驱动。匹配随机门控将选择与稀疏写入分离；在 GPT-2、TinyLlama 和 Mistral-7B 上的固定内部循环控制显示，内部循环写入强度依赖于模型；而 Mistral-7B 的匹配门控反转揭示了在驱动校准不当的情况下存在不对称的选择-驱动耦合。公共的 Memora 事件流用作外部诊断，暴露出陈旧记忆失效作为一个尚未解决的边界。在此探测中，选择性参数巩固提供了与检索访问不同且互补的记忆深度。

## Abstract
Long-running language agents need more than memory access. Retrieval systems can fetch past facts at query time, but they do not decide which experiences should continue to shape behavior after the working context is unloaded. We study this separate problem as memory depth: durable goal-conditioned tendencies written into a small parametric store. We introduce the loop-drift protocol, a controlled stress test in which the retrieval index remains intact while working context is unloaded and goal-conditioned behavior must persist under long-loop interference. We evaluate EVAF, a surprise- and valence-gated LoRA consolidation mechanism. Across GPT-2 and TinyLlama, retrieval is strongest on shallow factual recall (short-fact accuracy 0.956--0.973), while EVAF is strongest on goal persistence and post-unload recovery (0.812--0.904) with only 2--3 parametric writes per 200 events. Mechanism controls show that selective consolidation factorizes into two controllable dimensions: selection and actuation. Matched random gates isolate selection beyond sparse writing; fixed-inner controls across GPT-2, TinyLlama, and Mistral-7B show that inner-loop write strength is model-dependent; and a Mistral-7B matched-gate inversion reveals asymmetric selection-actuation coupling under miscalibrated actuation. Public Memora event streams serve as an external diagnostic, exposing stale-memory invalidation as an unresolved boundary. Within this probe, selective parametric consolidation supplies memory depth distinct from and complementary to retrieval access.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与研究动机  
- **核心问题**：长期运行的语言智能体不仅需要“记忆访问”（例如通过检索获取事实），更需要“记忆深度”——即在上下文被卸载后，过去经验仍能持续塑造行为，维持目标条件化的倾向。  
- **研究动机**：现有的检索增强生成（RAG）等方法擅长获取浅层事实，但无法决定哪些经验在不再出现在上下文时仍应影响行为。因此，作者将这种“写入小型参数存储的持久目标倾向”定义为**记忆深度**，并认为它与检索访问是互补但不同的问题。  
- **整体含义**：论文主张在长程代理设计中，应明确区分“事实检索”和“行为持久性”，并通过选择性参数巩固（而非大量写入）实现高效、低干扰的深层记忆。

### 2. 方法论核心思想与技术细节  
- **核心机制 EVAF**（基于惊讶和价态门控的 LoRA 巩固）：  
  - **惊喜门控**：对于每个事件，计算 token 负对数似然作为惊喜得分 \( s_t \)。  
  - **价态门控**：计算事件嵌入与用户持久目标/偏好嵌入的相似度作为价态得分 \( v_t \)。  
  - **联合准入分数**：\( g_t = \sigma(k_s(s_t - \tau_s)) \cdot \sigma(k_v(v_t - \tau_v)) \)，当 \( g_t > \tau_w \) 时事件被收入写缓冲区。  
  - **巩固更新**：缓冲区满后，使用缓冲区内事件及先前巩固事件回放，更新一个 LoRA 适配器，并加入 L2 正则化以限制漂移。  
- **选择与驱动的解耦**：  
  - **选择**：通过语义门控决定哪些事件被写入。  
  - **驱动**：通过内部循环步数控制写入强度，不同模型的最佳强度不同。  
- **算法直觉**：与朴素持续学习相比，EVAF 仅对行为相关事件进行稀疏更新，减少干扰漂移。

### 3. 实验设计  
- **合成场景——循环漂移协议**：专门构建，包含稳定目标事件、离题干扰、短暂相反请求、冲突、兄弟用户污染及事实笔记。每流 200 事件，10 用户，多种子。  
- **探测层**：  
  - 浅层事实（近期明确事实）  
  - 噪声事实（旧事实）  
  - 目标持久性（行为倾向持续）  
  - 卸载后恢复（上下文清空后立即探测，检索索引保留）  
- **对比方法**：  
  - 冻结模型（Frozen）  
  - 摘要（Summary）  
  - 检索增强生成（RAG）  
  - 朴素持续 LoRA（Naive-LoRA，每事件写入）  
  - EVAF  
  - 路由 EVAF+RAG（事实查询走检索，目标查询走 EVAF）  
- **外部边界诊断**：使用公共 Memora 事件流评估陈旧记忆失效问题。  
- **机制消融**：  
  - 匹配随机门控（与 EVAF 写入次数相同但随机选择事件）  
  - 固定内部循环步数控制（Fixed‑1、Fixed‑2、Fixed‑3）  
  - 行为边际控制器（概念验证）  
- **多模型测试**：GPT‑2、TinyLlama、Mistral‑7B。

### 4. 资源与算力  
- 论文中**未明确提及**具体 GPU 型号、显卡数量或训练耗时。仅说明使用了 GPT‑2、TinyLlama 和 Mistral‑7B 三种规模模型，实验基于 4 种子平均。因此算力细节缺失，无法估算计算资源消耗。

### 5. 实验数量与充分性  
- **实验组数**：  
  - 主要的深度翻转表（Table 2）展示 2 种模型、6 种方法、4 个指标。  
  - 驱动审计表（Table 3）对 2 种模型进行固定步数扫描（包括 Audit‑5、Fixed‑1/2/3、Margin）。  
  - Mistral‑7B 固定内部审计表（Table 1）及匹配门控反转实验。  
  - 边界 Memora 诊断（Table 4）。  
  - 多次种子平均（4 种子）和独立审计重运行，保证比较的统计可靠性。  
- **充分性与公平性**：  
  - 实验设计围绕“记忆深度”概念，通过受控合成协议隔离干扰、保持检索索引完整，公平对比各方法的深度能力。  
  - 消融实验细致（匹配随机门控、固定步数控制、不同模型规模），能够分离选择与驱动的作用。  
  - 但合成数据可能无法完全代表真实长对话的复杂多样性，Memora 诊断不显著亦表明对更新/删除的覆盖不足。

### 6. 主要结论与发现  
- **检索与参数巩固各司其职**：检索在浅层事实访问上具有绝对优势（短事实准确率 0.956–0.973），而 EVAF 在目标持久性和卸载后恢复上显著领先（0.812–0.904），且写入次数极少（每 200 事件约 2–3 次触发）。  
- **记忆深度可分解为选与驱**：选择性（语义门控）和驱动（内部步数）是两个可分离的控制维度；仅稀疏写入不够，还需要语义相关性。  
- **驱动强度模型依赖**：固定内部步数审计显示，不同模型需要不同的写入强度才能平衡目标行为与污染；过强的驱动会损害行为，甚至导致行为反转入侵。  
- **选择与驱动在线耦合**：驱动会影响未来的选择（通过改变模型状态而改变惊喜得分），这在大模型上尤其明显。  
- **陈记记忆删除未解决**：Memora 测试显示 EVAF 在陈旧记忆失效上表现不显著，表明未来需要有效性门控或重巩固机制。

### 7. 优点  
- **概念区分清晰**：明确界定“记忆访问”与“记忆深度”，为长期代理记忆研究提供了明确靶向。  
- **可分解的机制**：将选择性巩固分解为选择和驱动两个维度，并通过精确消融证明，增强了方法论的可解释性。  
- **受控应力测试协议**：循环漂移协议设计了多种干扰类型并保持检索索引完好，能够干净地测量卸载后的行为持久性。  
- **跨模型验证**：从 GPT‑2 到 Mistral‑7B，展示了结论的尺度扩展性，同时暴露了大规模下驱动校准的重要性。  
- **低资源消耗**：EVAF 用极少的参数写入（每 200 事件仅 2–3 次）实现高效记忆深度，实际部署成本低。

### 8. 不足与局限  
- **合成数据主导**：核心实验基于合成事件流，缺乏真实长对话的语义多样性和噪声分布，普适性待检验。  
- **遗忘能力缺失**：Memora 边界诊断显示 EVAF 无法有效处理陈旧记忆的删除或更新，这是长期记忆系统的核心缺口。  
- **驱动校准需手动**：最佳驱动强度因模型而异，目前需通过审计确定，缺乏自适应校准方法。  
- **大模型下的污染风险**：强驱动下兄弟污染指标饱和（如 Mistral 的 Fixed‑1 污染率达到 1.0），可能牺牲行为选择性。  
- **算力与效率报告不足**：未提及 GPU 类型、实验时长等实际资源情况，难以评估其可复现性和扩展成本。  
- **路由组合属概念验证**：EVAF+RAG 路由组合的表现存在模型依赖，实际组合策略仍需细化。

（完）
