---
title: Rapid Prototyping of Event-Driven Contextual Memory in the ACT-Up Cognitive Architecture
title_zh: 在ACT-Up认知架构中事件驱动上下文记忆的快速原型开发
authors: "Robert Thomson, Christian Lebiere"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28045v1"
tags: ["query:agent-memory"]
score: 9.0
evidence: 在认知架构中实现情境记忆以支持快速原型开发
tldr: 在认知建模中，ACT-Up架构虽然支持快速原型设计，但缺乏上下文记忆与事件处理能力，限制了复杂认知任务的模拟。为此，本文扩展了ACT-Up，实现了理论中立的工作记忆与传播激活机制，并集成了事件处理器和基本联想学习。我们以Klein等人的序列记忆任务为例，展示了快速原型开发，并研究了不同序列呈现及自由/序列回忆条件下的连续效应变化；同时，借助生成式AI与事件处理器，实现了从论文方法自动生成认知实验。该工作显著降低了新用户的使用门槛，为事件驱动认知建模提供了灵活且可扩展的基础。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28045v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 897, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28045v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 877, \"height\": 764, \"label\": \"Figure\"}]"
motivation: 现有ACT-Up认知架构缺乏上下文记忆与事件处理能力，新用户上手难度大，难以满足快速原型认知模型的需求，限制了其在事件驱动建模中的应用。
method: 本文通过在ACT-Up中添加上下文记忆与事件处理器，实现了理论中立的工作记忆、传播激活和联想学习机制。
result: 以Klein等人的序列记忆任务为例，在不同回忆条件下展示了连续效应变化，并利用生成式AI与事件处理器自动生成实验。
conclusion: 该工作显著降低了新用户使用ACT-Up的门槛，提升了快速原型能力，为事件驱动的认知模型构建提供了灵活而可扩展的基础。
---

## 摘要
本文描述了ACT-Up认知架构中上下文记忆和基本事件处理器的实现，该架构在保持其可扩展性和适合快速原型开发的同时，增加了基本功能并降低了新用户的入门门槛。这包括描述工作记忆和扩散激活的理论中立实现，以及基本的联想学习机制。使用Klein、Addis和Kahana（2005）描述的序列记忆任务，展示了用于算法开发的快速原型开发示例。该研究描述了在三种序列回忆和自由回忆条件下，序列列表呈现过程中邻近效应如何变化。我们进一步描述了如何使用生成式AI和事件处理器，直接从研究论文的方法部分自动创建认知实验。

## Abstract
The present paper describes an implementation of contextual memory and a basic event-handler for the ACT-Up cognitive architecture which maintains its scalability and appropriateness for rapid-prototyping while adding essential features and lowering the barrier to entry for new users. This includes describing a theory-neutral implementation of working memory and spreading activation, in addition to a basic associative learning mechanism. An example of rapid prototyping for algorithm development is presented using the serial memory task described in Klein, Addis, and Kahana (2005). This study describes how contiguity effects change across sequential list presentations across three serial and free recall conditions. We further describe how to use generative AI and the event handler to automatically create cognitive experiments directly from the Methods section of research papers.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化、深入、客观的总结。

### 1. 论文的核心问题与整体含义

*   **研究动机与背景**：
    *   **问题定位**：在认知建模领域，ACT-R等完整认知架构虽然功能强大，但其复杂的缓冲区和产生式系统等核心机制，导致模型在规模化（集成到更大、更长运行时间的模型）时遇到困难，并且不利于快速原型开发。
    *   **现有方案的不足**：为应对上述问题而开发的ACT-Up架构虽然实现了可扩展的核心声明性记忆，却牺牲了部分关键功能，如**扩散激活**、**上下文记忆**（即工作记忆与联想学习的结合）以及**事件处理**能力。这些缺失使其在模拟人类认知实验（尤其是捕捉邻近效应等）方面能力受限，且对新用户不够友好。
    *   **整体含义**：本论文的核心目标是填补这一空白，通过在ACT-Up中实现**可扩展的上下文记忆模块**和**基本的事件处理系统**，在保持其轻量、可扩展和适合快速原型开发等核心优势的同时，为其赋予模拟更复杂、更符合人类表现的认知任务的能力，从而降低新学者的入门门槛。

### 2. 论文提出的方法论

*   **核心思想**：
    *   **理论中立与模块化**：设计并实现一个不依赖于特定理论假设的上下文记忆模块和事件处理器，将实现细节（如衰减函数、干扰函数、扩散函数）留给建模者，通过与用户自定义函数配合，实现高度灵活的认知模型组件。
    *   **专注可扩展性**：所有新增机制（联想学习、上下文更新）都设计为在计算复杂度上可忽略不计，确保不会损害ACT-Up原有的可扩展性优势。

*   **关键技术细节**：
    *   **上下文记忆模块**：该模块是工作记忆和联想学习的结合体。
        *   **工作记忆API**：提供了一套函数来维护一个“上下文”元素集合。其核心是`update-context`函数，它应用一个用户提供的更新函数（默认为下文提到的衰减激活公式）和一个可选的阈值，来决定何时将衰减到一定程度的元素移出工作记忆。
        *   **联想学习API**：使用哈希表来存储从源元素到目标元素的有向联想及其强度，实现常数时间访问。核心函数`update-from-context`用于在检索后，创建或更新当前工作记忆中所有元素与检索到的目标元素之间的联想强度。
        *   **原型衰减函数**：
            *   `actr-decay-activation`：模拟经典ACT-R的简化基级衰减，公式为 $1 - d \times \log(1 + (t - t0))$，其中 $d$ 为衰减率（默认0.35），$t$ 为当前时间，$t0$ 为元素加入时间。
            *   `adaptive-decay-activation`：在基级衰减中引入干扰效应，衰减率会随工作记忆中元素数量 $|\text{context}|$ 的增加而动态增大，公式为 $1 - \frac{\log(|\text{context}|)}{w} \times \log(1 + (t - t0))$，其中 $w$ 控制干扰的影响程度。
        *   **原型扩散函数**：`capped-spread` 函数通过一个偏移的双曲正切变换 $\max * \tanh(b * \text{freq})$，将从各源元素传入的原始总扩散激活值归一化到 $[0, \max]$ 范围内，实现受约束的激活增长。
    *   **事件处理系统**：设计了一个简化的、类似认知心理学实验流程的事件系统。其API（`queue-item`, `show-item`, `advance-event-clock`等）允许用户方便地为模型安排带有明确开始（`onset-time`）和结束（`offset-time`）时间的刺激呈现任务。
    *   **间距效应与基级激活**：为应对重复学习时基级激活无限增长的问题，模型集成了一种适应性机制。每次呈现记忆时，其权重 $w_j$ 会被设定，以使该记忆在特定期望的短时时间范围 $t$ 后的基级激活 $B_i$ 能回到一个目标水平 $c$。公式为 $B_i = \log(\sum_{j=1}^{n} w_j * t^{-d_j})$。

### 3. 实验设计

*   **数据集/场景**：论文以 **Klein, Addis, & Kahana (2005)** 的序列记忆实验为案例进行复现。该实验要求参与者学习一个19词列表，该列表在不同条件下重复呈现5次。
*   **Benchmark（基准任务）**：核心任务是预测在该多呈现记忆范式下的人类行为数据，具体指标包括：
    *   **条件反应概率**：衡量在回忆出项目 $i$ 之后，回忆出项目 $i + \text{lag}$ 的概率，用于捕捉邻近效应（Contiguity Effects）。
    *   **序列位置曲线**：衡量列表不同位置项目的回忆准确率，捕捉首因效应（Primacy Effect）和近因效应（Recency Effect）。
*   **对比方法**：
    *   **与人类数据对比**：将模型模拟结果直接与Klein等人（2005）观察到的三种实验条件（`serial-constant`, `free-constant`, `free-varied`）下的人类数据进行定性及定量比较（$r^2 > 0.9$）。
    *   **与既有模型对比**：在讨论部分，将本模型的理论机制与TCM/CMR（时序上下文模型）、SEM（起始-终点模型）、SIMPLE（时间比率模型）等经典记忆模型进行对比分析。
    *   **与前序工作对比**：与Thomson等人（2015）的模型进行对比，指出本模型没有采用心理上不现实的“声明性FINSTs”等策略，也同样取得了可比拟的性能。

### 4. 资源与算力

*   论文中**未明确提及**所使用的GPU型号、数量等硬件算力信息。
*   文中仅提供了模型运行的时间信息作为效率参考：本模型运行**100次重复的单次实验**大约耗时**30秒**，其中大量的时间开销用于记录日志。这暗示了模型本身计算量小，可在普通计算机上快速运行，符合其“快速原型”的设计目标。

### 5. 实验数量与充分性

*   **实验数量**：论文主要报告了一个核心实验（Klein等人，2005序列记忆任务），但该实验是相当全面和立体的。
    *   **条件覆盖**：模型模拟了该任务的所有**3种实验条件**（`serial-constant`, `free-constant`, `free-varied`）。
    *   **时间维度**：分析了在**5次重复列表呈现**过程中（文中展示了第1、3、5次），模型行为的变化。
    *   **迭代次数**：每种条件下，模型被运行了**100次迭代**以保证结果的稳定性。
*   **充分性评估**：
    *   **优点**：实验设计对于验证其核心论点（多呈现序列记忆中的邻近效应演变）是充分且聚焦的。模型对CRP数据的拟合度很高（$r^2 > 0.9$）。
    *   **局限性**：论文缺乏**消融实验**来量化各个新模块（如不同衰减函数、扩散函数、间距效应机制）的独立贡献。同时，仅验证了一个单一实验范式，其在不同任务上的泛化能力尚待证明。因此，从整个架构的评价来看，实验的**广度略显不足**。

### 6. 论文的主要结论与发现

*   **功能性结论**：成功地为ACT-Up架构开发了一个**可扩展且理论中立的上下文记忆和事件处理系统**，并证明了其在模拟人类认知实验方面的有效性。
*   **模型表现结论**：通过使用`capped-spread`扩散函数和`actr-decay-activation`工作记忆衰减函数，新模型在无需借助心理现实性存疑的策略（如声明性FINSTs）的前提下，**准确预测了序列记忆任务中的条件反应概率**（$r^2 > 0.9$）。
*   **机制性结论**：
    *   **临近效应**由非对称的联想扩散所驱动，在序列条件中最为明显，且在自由变化条件下随着呈现次数增多而减弱。
    *   **近因效应**由工作记忆中激活的衰减驱动，**首因效应**则由工作记忆中的“起始”概念作为上下文引发的联想启动得到增强。
    *   **间距效应的实现**成功平衡了基级激活与扩散激活，避免了重复学习时的激活失控。
*   **方法论结论**：展示了**利用生成式AI**（如Perplexity）直接从研究论文的方法部分**自动生成实验代码骨架**的可行性，进一步提升了快速原型开发的效率。
*   **理论性结论**：该框架为TCM、SIMPLE、SEM等不同序列记忆理论之间提供一个可行的综合桥梁，特别是在解释多呈现范式下的行为数据方面。

### 7. 优点

*   **高实用性与低门槛**：为ACT-Up补充了从建模认知实验角度而言必不可少的组件，并通过提供原型函数和事件系统，极大降低了新用户的使用难度。
*   **极佳的可扩展性**：对新功能的算法复杂度进行了分析，确认了它们在大型模型中的开销可忽略不计，成功兑现了“可扩展”的承诺。
*   **设计灵活且模块化**：API设计允许用户通过替换关键函数（如衰减、扩散、联想更新函数）来快速测试不同理论假说，完美支持“快速原型”的目标。
*   **创新的方法论结合**：将认知架构与生成式AI相结合来自动化实验创建过程，是一个极具前瞻性和实用价值的方法尝试。

### 8. 不足与局限

*   **实验验证单一**：整个论文仅通过一个认知任务（序列记忆）来验证所提出的复杂框架，缺乏在其他经典范式（如N-back、复杂广度任务）上的测试，模型的泛化能力有待检验。
*   **缺乏消融实验**：虽然介绍了多种机制（如两种工作记忆衰减、间距效应），但未通过消融实验系统地展示每个组件对最终结果的独立贡献和必要性。
*   **拟合不够全面**：模型结果仅重点展示了CRP数据的拟合，而对另一关键指标——“序列位置曲线”的拟合情况仅停留在文字描述，缺少可视化图表和定量指标。
*   **与人类数据和理论的对比深度有限**：虽然CRP拟合度高（$r^2 > 0.9$），但可能只是在一个维度上的高拟合。缺乏对反应时、个体差异等更丰富行为数据的对比，与其他理论模型的定量比较也不够深入。
*   **实现细节尚不完整**：论文中提到的“起始”（start）概念虽然比SEM的“起止标记”更合理，但仍是一个为解释首因效应而引入的策略性设定，其本身的理论基础和实践一致性有待进一步探讨。

（完）
