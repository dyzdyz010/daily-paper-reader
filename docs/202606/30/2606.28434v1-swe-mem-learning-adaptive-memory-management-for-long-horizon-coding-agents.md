---
title: "SWE-MeM: Learning Adaptive Memory Management for Long-Horizon Coding Agents"
title_zh: SWE-MeM：学习长程编码智能体的自适应记忆管理
authors: "Shuzheng Gao, Wenhao Zeng, Zhaojian Yu, Jianqiao Wangni, Chaozheng Wang, Kai Cai, Shilin He, Michael R. Lyu"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28434v1"
tags: ["query:agent-memory"]
score: 10.0
evidence: 提出编程智能体的自适应记忆压缩训练框架，优化记忆管理时机与方式。
tldr: "长期软件工程代理面对有限上下文预算，需管理冗长且噪声干扰的交互历史。现有内存管理多采用静态压缩或刚性约束时机与粒度，无法联合优化内存利用与问题解决。SWE-MeM训练框架提供灵活内存工具，让代理依据轨迹状态、任务进度和剩余预算自主决定压缩时机、内容及方式，并利用合成轨迹与Memory-aware GRPO联合训练内存管理与任务求解。在SWE-Bench Verified上，4B和30B模型分辨率分别达43.4%和60.2%，在性能和令牌效率上超越基线，首次实现主动按需内存管理，显著提升长程编程代理能力。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28434v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1707, \"height\": 474, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28434v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1699, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28434v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1705, \"height\": 821, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.28434v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1726, \"height\": 428, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28434v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1718, \"height\": 1599, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28434v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1718, \"height\": 627, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28434v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 867, \"height\": 281, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28434v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 627, \"height\": 411, \"label\": \"Table\"}]"
motivation: 长期代理受上下文预算限制，现静态压缩方法缺灵活性，未联合优化内存与问题解决，制约长程任务表现。
method: 提出SWE-MeM训练框架，赋予代理灵活内存工具自主压缩，并通过合成轨迹与Memory-aware GRPO联合优化内存管理与问题求解。
result: "在SWE-Bench Verified上，SWE-MeM以4B和30B模型分别达43.4%和60.2%解决率，性能与效率均超越现有基线。"
conclusion: SWE-MeM实现主动按需内存管理，有效提升长程软件工程代理的性能与效率。
---

## 摘要
长程软件工程智能体通常需要在有限的上下文预算下管理冗长且嘈杂的交互历史。现有的记忆管理方法通常依赖静态压缩流程，或对压缩时机和粒度施加严格限制。此外，这些方法未能将记忆管理与问题解决能力联合优化，以在提升性能的同时减少 token 使用量。我们提出 SWE-MeM，一个用于软件工程智能体中主动型与按需记忆管理的训练框架。SWE-MeM 提供了一种灵活的记忆工具，使智能体能够根据轨迹状态、任务进展和剩余上下文预算，决定何时、压缩什么以及如何压缩。我们使用合成的主动记忆管理轨迹和 Memory-aware GRPO（记忆感知的 GRPO）来训练智能体，该方法通过记忆感知的轨迹分割和步级信用分配，联合优化记忆管理和问题解决。在 SWE-Bench Verified 上，SWE-MeM 分别使用 4B 和 30B 模型达到了 43.4% 和 60.2% 的解决率，在性能和效率上均优于现有的记忆管理基线。

## Abstract
Long-horizon software engineering agents often need to manage lengthy and noisy interaction histories under limited context budgets. Existing memory management methods typically rely on static compression workflows or impose rigid constraints on compression timing and granularity. Moreover, these approaches fail to jointly optimize memory management and issue resolution capabilities to improve performance while reducing token usage. We present SWE-MeM, a training framework for proactive and on-demand memory management in software engineering agents. SWE-MeM provides a flexible memory tool that lets agents decide when, what, and how to compress based on trajectory state, task progress, and remaining context budget. We train agents with synthesized proactive memory-management trajectories and Memory-aware GRPO, which jointly optimizes memory management and issue resolution through memory-aware trajectory splitting and step-level credit assignment. On SWE-Bench Verified, SWE-MeM achieves 43.4% and 60.2% resolve rate with 4B and 30B models, respectively, outperforming existing memory management baselines in both performance and efficiency.

---

## 论文详细总结（自动生成）

好的，以下是对该论文的结构化深入总结。

### 1. 论文的核心问题与整体含义
*   **研究动机**：在解决长程（Long-Horizon）软件工程任务（如修Bug）时，智能体（Agent）会通过多轮交互（如读文件、运行测试）产生冗长的历史记录。这带来了两个核心挑战：一是交互历史可能超出大语言模型（LLM）的上下文窗口限制；二是即便不超出，长而嘈杂的上下文也会分散模型注意力，使其难以有效利用关键信息。
*   **核心问题**：现有的记忆管理方法（如达到上下文长度阈值时强制总结、或按子任务边界折叠历史）存在两大局限。第一，**灵活性不足**：它们使用静态、固定的压缩规则，无法根据信息的重要性动态决策（例如，冗长的执行日志可以随时安全压缩，而早期检查的代码可能对后续推理至关重要）。第二，**优化目标单一**：现有方法通常只优化上下文长度的缩短，而非与任务解决效率（性能、Token消耗、交互步数）联合优化，有时反而导致总Token消耗或交互步数激增。
*   **整体含义**：本论文旨在提出一种训练框架，让智能体能够**学习一种主动且按需（Proactive and On-demand）的记忆管理策略**，自主决定何时压缩、压缩什么、如何总结，从而在提升任务解决率的同时，实现更高的整体效率。

### 2. 方法论
SWE-MeM的核心思想是赋予智能体一个灵活的记忆管理工具，并通过课程学习和强化学习训练其有效使用该工具的能力。整个训练流程如图3所示，包含三个关键部分：

*   **灵活的记忆管理工具**：
    *   智能体可以随时调用一个`compress`工具，参数包括：`analysis`（当前状态分析）、待压缩的起止步数(`start_step_number`, `end_step_number`)、压缩后的内容(`content`)和剩余工作计划(`remaining_work`)。
    *   该操作会用一个精炼的摘要替换掉选定的轨迹片段，从而按需、动态地缩短上下文，而非一次性压缩全部历史。

*   **课程微调与轨迹合成**：
    *   **轨迹合成**：为了提供训练数据，使用一个专有模型（如GPT-5.1）作为“老师”，在基础模型生成的轨迹中模拟记忆管理动作。触发时机有两种：1) **上下文预算压力** (接近长度限制时)；2) **主动记忆管理** (检测到子任务完成、信息密度低、或代理注意力分散时)。
    *   **质量过滤**：通过基于规则的清理（如过滤掉压缩率过高或过低的动作、无效的工具调用等）来保证训练样本质量。
    *   **课程学习**：分两阶段训练。第一阶段学习基本的记忆工具格式和用法；第二阶段通过提取和筛选高质量的主动记忆管理样本（如检查“分析”与“压缩内容”是否一致），专门强化模型主动压缩的能力。

*   **记忆感知的GRPO (Memory-aware GRPO)**：
    *   这是对标准GRPO强化学习算法的改进，以更好地适应记忆管理场景。主要包含两个机制：
        *   **记忆感知的轨迹分割与损失聚合**：在一次压缩动作后，将完整的交互轨迹分割成多个子轨迹（Sub-trajectories），确保训练和推理时模型观测到的上下文状态一致。同时，为避免长短不一的轨迹对优化产生偏差，提出了两阶段损失聚合方法（公式3），先平均每个轨迹内所有有效Token的损失，再平均批次内所有轨迹的损失，使每个序列对总损失的贡献相等。
        *   **步级信用分配掩码**：不同于仅根据最终任务结果分配奖励，该方法引入了步级掩码（公式4中的 `m_{i,k,t}`）。它会掩盖掉不恰当的压缩行为（如压缩率不当、延迟压缩、导致上下文溢出的步骤等），从而提供更精细的信用分配，让模型学会优质的记忆管理策略。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：从 SWE-ReBench 和 SWE-Gym 数据集中构造。SFT（监督微调）阶段使用合成的记忆管理轨迹；RL（强化学习）阶段进一步筛选出成功/失败样本混合的非平凡子集进行训练。
    *   **评估基准**：主要基准是 **SWE-Bench Verified**。此外，还在更具挑战性的 **SWE-bench Multilingual**（多语言编程）和 **SWE-bench Pro**（更长程任务）上进行了泛化性测试。
*   **对比方法**：论文进行了详尽的对比，主要分为三类：
    1.  **无记忆管理的ReAct智能体**：包括同为4B和30B级别的多个强基线模型，如Qwen3系列、SWE-Gym、SWE-Mirror、SWE-Lego等。
    2.  **现有记忆管理方法**：
        *   **摘要型智能体**：基于阈值触发的压缩方法。
        *   **折叠型智能体**：SWE-Compressor 和 Context Folding等方法。
    3.  **SWE-MeM自身的变体**：例如，仅在推理时使用记忆管理工作流而不训练的版本（Workflow-only）、仅SFT版本和完整的SFT+RL版本。

### 4. 资源与算力
*   **论文未明确说明具体的GPU型号、数量及总训练时长。**
*   文中提到了部分训练配置：
    *   **基座模型**：使用了 Qwen3-4B-Instruct 和 Qwen3-Coder-30B-A3B。
    *   **SFT（监督微调）**：全局批次大小为128，训练3个轮次。第二阶段的课程学习训练1个轮次。
    *   **RL（强化学习）**：批次大小为64，每个实例采样8条轨迹，最大交互步数设置为150步。
    *   **上下文限制**：整个数据收集到训练过程，上下文窗口均设置为32,768个Token。

### 5. 实验数量与充分性
*   **实验数量**：实验设计比较充分。论文进行了**主要性能对比**（表1，对比超过15个基线和变体）、**效率分析**（表2）、**消融研究**（图4，探究了主动触发、选择性压缩、课程SFT、记忆感知GRPO等各组件的作用）、**泛化性测试**（表3，在两个额外基准上测试）以及详细的**案例分析**（附录C）。
*   **实验的充分性与公平性**：
    *   **客观公平**：对比方法涵盖了当时的主流方案。为公平比较，统一基于OpenHands脚手架，并将所有方法的上下文限制在相对紧凑的32K Token下进行评估。
    *   **充分性**：通过消融实验清晰地验证了SWE-MeM各个核心设计（主动触发、选择性压缩、课程学习、记忆感知GRPO）的贡献。不仅报告了解决率，还深入分析了Token用量和交互步数，提供了更全面的效率视角。在两个额外的长程/多语言基准上的测试也初步证明了方法的泛化能力。

### 6. 主要结论与发现
*   **性能优异**：在32K上下文预算下，SWE-MeM在SWE-Bench Verified上取得了领先的解决率，分别达到43.4%（4B模型）和**60.2%**（30B模型），大幅超越各基线模型。
*   **效率优势**：SWE-MeM在达到最佳性能的同时，保持了较低的Token消耗。与导致总Token量增加的其他记忆管理方法不同，SWE-MeM甚至比对应的ReAct基座模型消耗更少的Token，交互步数的增加也控制得更好。
*   **组件有效性**：消融研究证明，**主动触发**（相比被动阈值触发）和**选择性压缩**（相比全历史压缩）是性能的关键；课程SFT和记忆感知GRPO均能带来一致的提升。
*   **超越防溢出**：实验表明，即使为训练好的模型提供更大的256K上下文并移除记忆工具，其性能也会大幅下降。这揭示了记忆管理的价值不仅仅是防止上下文溢出，更重要的是**提供了一个更清晰、聚焦的工作环境，帮助模型更有效地利用任务相关信息**。
*   **泛化能力**：在多语言和更长程的编程任务上，SWE-MeM同样展现出一致的提升效果，表明其学习到的记忆管理行为可以跨语言、跨任务迁移。

### 7. 优点
*   **思想创新**：从“被动、遵循规则”的记忆管理转向“主动、学习驱动”的模式，赋予了智能体按需管理其上下文的元认知能力。
*   **技术完整**：形成了一套从数据合成、课程微调到专门强化学习算法的完整训练框架。Memory-aware GRPO中的轨迹分割和步级掩码设计精巧，充分考虑到了记忆管理动作对上下文的特殊影响。
*   **评估全面**：不仅关注任务成功率，还深入分析了Token开销和交互步数，并通过多种消融实验和泛化性测试对方法进行了充分的验证和剖析，结论令人信服。
*   **实用性强**：在较低的4B模型和紧凑的32K上下文限制下也能取得很高的性能，降低了对大模型海量上下文窗口的依赖，对于实际部署应用有重要价值。

### 8. 不足与局限
*   **计算资源开销不透明**：论文全程未提及训练所耗费的具体算力（GPU型号/数量/时间），这使得其他研究者难以评估其复现成本和可扩展性。
*   **偏重特定领域**：方法的验证主要集中在软件工程（SWE-Bench）任务上，对于其他类型的长程智能体任务（如网页交互、办公自动化）的泛化性尚待考证。
*   **对合成数据的依赖**：SFT阶段的训练完全依赖于专有模型（GPT-5.1）合成的记忆管理轨迹。虽然RL阶段会进一步优化，但初始能力的质量可能会受到合成模型能力的“天花板”限制，即老师模型理解何时该压缩的能力上限决定了学生模型的性能上限。
*   **步级掩码设计的复杂性**：步级信用分配逻辑依赖多个规则化的质量检查项（如压缩比例、范围对齐等），这些规则的设定本身可能引入人工偏差，且其泛化性可能受限。

（完）
